# Core data handling
pandas
numpy
scipy
openpyxl          # .xlsx read/write
pyarrow           # .parquet output in scripts/stats/

# Statistics
statsmodels

# Plotting
matplotlib
seaborn
PyYAML            # colors.yaml
svgutils          # scripts/figures/combine_figure.py
PyMuPDF           # `fitz`, PDF handling in figure composition
Pillow            # raster embedding in combined figures

# LLM providers (only the ones you actually run are required)
openai            # GPT models + any vLLM/OpenAI-compatible endpoint
anthropic         # Claude models
google-genai      # Gemini models
requests          # academic_ai.py client
python-dotenv     # loads .env
